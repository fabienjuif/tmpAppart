{
  "name": "@tmpAppart/workspace",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "run-p start:*",
    "start:ui": "yarn --cwd ui start",
    "deploy": "run-p deploy:*",
    "deploy:api": "yarn --cwd api deploy"
  },
  "workspaces": [
    "api",
    "ui"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
